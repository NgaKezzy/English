import 'package:flutter/material.dart';

class EditSubVideoScreen extends StatelessWidget {
  const EditSubVideoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
